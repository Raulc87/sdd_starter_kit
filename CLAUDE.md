# CLAUDE.md

Read `AGENTS.md` first and follow it as the authoritative agent workflow.

Use `PROJECT_CONTEXT.md` as the navigation map for the active project documentation.

Do not create a parallel process that conflicts with `AGENTS.md`.
